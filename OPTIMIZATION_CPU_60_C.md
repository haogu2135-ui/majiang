# CPU Optimization Ledger: Batch C

This ledger records the third batch of CPU-focused optimizations. The source
of truth is the `.gd.part` file named in each row; `scripts/main.gd` is
assembled output.

| # | File / function | Previous hotspot | Optimization | Verification |
|---:|---|---|---|---|
| 70 | `core.gd.part` / `can_form_sets_with_memo` | Every recursive set-formation state rescanned tile counters from slot zero. | Pass the current first candidate slot into recursive calls and scan only the remaining suffix. | `performance_optimization_60_c_check.gd`, `ai_play_round25_check.gd` |
| 71 | `ai_brain.gd.part` / `hand_plan_features_from_counts` | Each repeated route evaluation rescanned all 34 counters and rebuilt the feature dictionary. | Add a bounded linked-list LRU keyed by count vector and explicit tile total; return isolated copies to callers. | `performance_optimization_60_c_check.gd`, `ai_play_round87_check.gd`, `ai_play_round97_check.gd` |
| 72 | `ai_brain.gd.part` / `opponent_pressure_score` | Discard and gang paths recomputed the opponent loop after pressure context construction. | Reuse the seat-matched `opponent_pressure` value already present in the evaluation context. | `performance_optimization_60_c_check.gd`, `ai_play_round80_check.gd` |
| 73 | `core.gd.part` / `full_straight_suit_from_counts` | Each candidate suit rescanned every open meld to find 123/456/789 groups. | Classify each eligible open meld once, then reuse the three-suit group table. | `performance_optimization_60_c_check.gd`, `ai_play_round25_check.gd` |
| 74 | `ai_brain.gd.part` / `opponent_readiness_pressure_score` | Readiness pressure was recomputed across every opponent when an evaluation context already held the pressure context. | Reuse the seat-matched cached `readiness_pressure` value and retain the live scan as a fallback. | `performance_optimization_60_c_check.gd` |
| 75 | `render.gd.part` / hand tray decoration | Suit-flow and completion-bus art independently regrouped the same hand during one render. | Compute the five group counts once in `draw_hand` and pass the snapshot to both decoration builders. | `ui_layout_smoke_test.gd` |
| 76 | `ai_brain.gd.part` / `opponent_readiness_pressure_score` | Context-free calls rebuilt opponent plan pressure for every invocation. | Populate and reuse the existing state-keyed opponent runtime snapshot before falling back to context-aware evaluation. | `performance_optimization_60_c_check.gd`, `ai_play_round80_check.gd` |
| 77 | `gameplay.gd.part`, `render.gd.part` / discard river grids | Every battle redraw recreated four grid containers and all visible discard tile controls. | Retain grids across battle redraws and reuse them only when river content, window, geometry, mode, and highlight signatures match; release stale grids immediately. | `performance_optimization_60_c_check.gd`, `ui_layout_smoke_test.gd` |
| 78 | `core.gd.part`, `ai_brain.gd.part` / shanten terminal branches | The recursive shanten search continued branching after four melds, and a standard win still scanned alternate hand families. | Return the exact four-meld result immediately and skip seven-pairs/thirteen-orphans scans after a standard `-1` result. | `performance_optimization_60_c_check.gd`, `offline_smoke_test.gd` |
| 79 | `main_base.gd`, `core.gd.part` / compact shanten keys | Every recursive memo-key lookup allocated a fresh byte buffer before converting the 34 counters to a string. | Reuse one script-level scratch buffer while retaining the returned immutable string keys. | `performance_optimization_60_c_check.gd`, `offline_smoke_test.gd` |
| 80 | `ai_brain.gd.part`, `core.gd.part` / winning wait valuation | Each wait candidate rebuilt and normalized the full simulated hand before scoring it. | Reuse the discard evaluator's 34-slot count snapshot and score winning candidates through the count-based API; retain the array path for legacy callers. | `performance_optimization_60_c_check.gd`, `ai_play_round80_check.gd`, `ai_play_round96_check.gd`, `ai_play_round97_check.gd` |
| 81 | `ai_brain.gd.part` / `build_ai_claim_report` | A context-free claim report evaluated the same seat-aware hand plan once for the decision score and again to read its bonus. | Reuse the already computed `before_plan_report` for `plan_bonus`, removing one full feature/report rebuild per standalone claim evaluation. | `ai_play_round3_check.gd`, `ai_play_round10_check.gd`, `performance_optimization_60_c_check.gd` |
| 82 | `gameplay.gd.part`, `render.gd.part` / seat panels | Every battle redraw recreated both seat panel subtrees for all four seats, including their static art, labels, and decorations. | Retain each unchanged panel/shadow pair across a battle redraw and reuse it only when the layout, table state, player summary, threat revision, and visual settings signature matches. | `performance_optimization_60_c_check.gd`, `ui_layout_smoke_test.gd` |
| 83 | `gameplay.gd.part`, `render.gd.part` / meld lanes | Every battle redraw recreated each populated meld lane, its group tile views, badges, lane art, and optional pager. | Retain the complete per-seat meld lane subtree and reuse it only when meld order/content, page window/capacity, safe-area geometry, compact mode, and visual settings match; stale lanes are released with the render retention set. | `performance_optimization_60_c_check.gd`, `ui_layout_smoke_test.gd` |
| 84 | `gameplay.gd.part`, `render.gd.part`, `wall_back_strip.gd` / wall back strips | Every battle redraw recreated the four fixed wall strip controls and re-ran their self-drawing pass, including when the wall visual state was unchanged. | Retain the four fixed-layout `WallBackStrip` instances, update count, ratio, low-wall, feedback, geometry, and capacity through `configure()`, and skip its redraw request when those visual inputs are unchanged; stale strips are released with the battle retention set. | `performance_optimization_60_c_check.gd`, `offline_smoke_test.gd`, `ui_layout_smoke_test.gd` |
| 85 | `gameplay.gd.part`, `render.gd.part` / table log ledger | Every battle redraw rebuilt the recent-record ledger, its two row entries, and the history button even when the visible log content and layout state were unchanged. | Retain the complete `TableLogLedgerPanel` subtree, including its existing history-button binding, and reuse it only when log contents, pending-response visibility, archive state, typography, viewport, and safe-area geometry match; invalidate it when a record changes. | `performance_optimization_60_c_check.gd`, `ui_layout_smoke_test.gd` |
| 86 | `gameplay.gd.part`, `render.gd.part` / last-discard focus marker | Every battle redraw rebuilt the latest-discard marker and its large decoration/tween subtree even when the same discard remained current. | Retain the complete `LastDiscardFocusMarker` subtree under a state signature covering tile, source seat, river count/geometry, response state, viewport, accessibility, and motion settings; restart its owner-bound animation on reuse and rebuild only when the latest-discard state changes. | `performance_optimization_60_c_check.gd`, `ui_layout_smoke_test.gd` |
| 87 | `gameplay.gd.part`, `render.gd.part` / discard river chrome | Every battle redraw recreated each river's decorative bed, owner badge, and the archive control's internal art even when the discard windows and visual state were unchanged. | Retain per-seat river art and owner overlays under geometry/content/accessibility signatures, and retain archive art inside the existing per-seat history button; reuse unchanged subtrees, restart archive animation for retained art, and release stale entries after each draw. | `performance_optimization_60_c_check.gd`, `ui_layout_smoke_test.gd` |
| 88 | `gameplay.gd.part`, `render.gd.part` / top battle HUD | Every full battle redraw rebuilt the complete top HUD shell, status art, hand-progress meter, wall meter, score strip, and action buttons even when their visible state was unchanged. | Retain the complete `TopHud3DShell` under a signature covering mode/rules, viewport and accessibility, phase/status, wall and room state, online score/connection state, update state, and HUD-visible animation inputs; reuse it with the live status reference restored, rebuild on signature changes, and restart owner-bound progress animations. | `performance_optimization_60_c_check.gd`, `ui_layout_smoke_test.gd` |
| 89 | `gameplay.gd.part`, `render.gd.part` / action chrome | Every battle redraw recreated the action dock shadow, surface, status slot, pulse driver, and normal-state intent strip even when the available action set and layout state were unchanged. | Retain the non-button action chrome under a signature covering action roles/text, response mode, recovery/focus status, viewport/accessibility, and animation inputs; keep action buttons rebuilt for fresh callbacks, reuse the dock/intent art when valid, and restart the dock pulse under its owner. | `performance_optimization_60_c_check.gd`, `ui_layout_smoke_test.gd` |
| 90 | `gameplay.gd.part`, `render.gd.part` / table living illustration | Every battle redraw recreated the static `TableLivingIllustration` host and its latest-discard ripple nodes even when the table geometry and latest discard were unchanged. | Retain the ripple host under a signature covering viewport/safe-area geometry, latest-discard tile/source, and FX state; reuse it on unchanged redraws and release it when the discard or geometry changes. | `performance_optimization_60_c_check.gd`, `ui_layout_smoke_test.gd` |
| 91 | `gameplay.gd.part`, `render.gd.part` / table chrome scaffold | Every battle redraw recreated the fixed table floor shadow, front apron, apron highlight, and cast-shadow hosts even when viewport-safe-area geometry was unchanged. | Retain the three root chrome hosts under a geometry signature, reuse them on unchanged redraws, and release the bounded set on mode changes or signature invalidation; dynamic board children remain independently retained. | `performance_optimization_60_c_check.gd`, `ui_layout_smoke_test.gd` |
| 92 | `gameplay.gd.part`, `render.gd.part` / wall feedback chrome | Every battle redraw rebuilt the low-wall/move feedback art and remaining-wall badge, including their atlas crops, labels, rails, and pulse decoration. | Retain feedback art and badge independently under a wall-count/progress/low-wall/recent-draw/FX signature, restart owner-bound feedback animation on reuse, and release stale visual state before the next draw. | `performance_optimization_60_c_check.gd`, `ui_layout_smoke_test.gd` |
| 93 | `gameplay.gd.part`, `render.gd.part` / AI advisor panel | Every battle redraw discarded and rebuilt the AI advisor side channel, its three reading cards, route art, and static textures even when advice and layout state were unchanged. | Retain the complete advisor panel under a hand/advice/threat/layout signature, restore the current UI root generation and advisor fingerprint on reuse, and leave dynamic detail surfaces independent. | `performance_optimization_60_c_check.gd`, `ui_layout_smoke_test.gd` |
| 94 | `gameplay.gd.part` / table surface scaffold | Every battle redraw recreated the outer table host, inner surface host, and perspective-depth carrier even when viewport-safe-area geometry and the active seat were unchanged. | Retain the complete three-node table surface scaffold under a bounded geometry/seat signature, reuse it only after data-bearing children have been removed to their own retention slots, and rebuild it on geometry or seat changes. | `performance_optimization_60_c_check.gd`, `ui_layout_smoke_test.gd` |
| 95 | `gameplay.gd.part`, `render.gd.part` / online chat action button | Every complete online battle redraw recreated the auxiliary chat button and its authored action-button art even when chat state, pending-response density, and visual settings were unchanged. | Retain the complete `ChatActionButton` under a bounded online/layout/chat/pending-mode signature, reuse it with its existing callback, and rebuild it when chat visibility, response density, or accessibility/motion settings change. | `performance_optimization_60_c_check.gd`, `ui_layout_smoke_test.gd` |
| 96 | `gameplay.gd.part`, `render.gd.part` / normal offline action bar | Every ordinary offline battle redraw recreated the action row, native action buttons, authored button art, and callback closures even when the hand, rule, AI recommendation, and phase were unchanged. | Retain the complete normal offline action bar under an independent hand/rule/AI/phase signature, remount it with its existing callbacks, and rebuild it for pending claims, danger confirmation, settlement, restart confirmation, or changed action state. | `performance_optimization_60_c_check.gd`, `ui_layout_smoke_test.gd` |
| 97 | `gameplay.gd.part`, `render.gd.part` / round summary modal | Every complete refresh after a round ended rebuilt the full-screen input shield, settlement panel, win detail, score-flow art, body scroll area, and four ranking rows even when the finished-round state was unchanged. | Retain the paired summary input shield and `RoundSummaryPanel` under a settlement-data, ranking, geometry, accessibility, and animation-settings signature; remount both with the external action lane and body status reference preserved, and release the pair together when the result changes or the mode leaves settlement. | `performance_optimization_60_c_check.gd`, `ui_layout_smoke_test.gd` |
| 98 | `main_base.gd`, `gameplay.gd.part`, `render.gd.part` / retained settlement animation lifecycle | Retained settlement nodes could keep detached particle/tween work alive, and `clear_screen()` could leave a reused panel, rank row, score text, or preview at an interrupted animation state. | Bind settlement and preview tweens to their owning subtrees, clear the detached panel's screen/particle effects before remount or release, and normalize animated transforms, opacity, progress, and score labels to their stable final state; replace the untracked summary number roll with owner-bound callbacks. | `performance_optimization_60_c_check.gd`, `ui_layout_smoke_test.gd`, `git diff --check` |
| 99 | `ai_brain.gd.part` / AI ron decision report | One ron decision rebuilt the same concealed-hand counts for furiten, win validation, scoring, and effective-wait analysis, while alternate waits rebuilt each candidate hand array. | Build one concealed-hand count snapshot, reuse it across the validation/furiten/effective-wait paths, add only the winning tile for scoring, and probe alternate waits by mutating one reusable count copy; preserve the completed self-draw furiten fallback and all rejection reasons. | `performance_optimization_60_c_check.gd`, `ai_play_round40_check.gd`, `ai_play_round41_check.gd`, `ai_play_round42_check.gd` |
| 100 | `ai_brain.gd.part` / discard report evaluation | Quiet candidate ranking and complete report construction repeated the same candidate shanten and risk-vector reads; every report also re-read seat-static meld and profile values. | Carry the quiet ranker's shanten and complete risk vector into the report, and snapshot exposed meld count plus attack, route, and risk multipliers in the shared evaluation context with fallbacks for legacy callers. | `performance_optimization_60_c_check.gd`, `ai_play_round100_check.gd` |
| 101 | `ai_brain.gd.part` / AI tsumo decision report | One self-draw decision rebuilt counts for win validation and scoring, rebuilt the pre-draw hand for effective waits, and appended every alternate wait to a temporary hand array for scoring. | Reuse one complete-hand count snapshot, derive the pre-draw count vector by removing the recorded draw, score the current and alternate waits through the count API, and retain the array path only for the required wait presentation and draw removal. | `performance_optimization_60_c_check.gd`, `ai_play_round101_check.gd` |
| 102 | `ai_brain.gd.part` / self-gang effective-count comparison | A concealed self-gang report already prepared before/after count vectors and shanten values, then `effective_tile_count` rebuilt both vectors and recomputed their shanten during wait-narrowing comparison. | Allow `effective_tile_count` to consume known shanten and count snapshots, and pass the prepared before/after state from the concealed-gang report while preserving the legacy helper path for other callers. | `performance_optimization_60_c_check.gd`, `ai_play_round102_check.gd`, `ai_play_round67_check.gd` |
| 103 | `ai_brain.gd.part` / claim-time ron report hand setup | `choose_ai_claim` already built a claimant's count vector for claim options, but the ron decision report rebuilt the same concealed-hand counts before evaluating the response. | Add a backward-compatible count snapshot parameter to `ai_ron_decision_report` and pass the existing vector from claim selection; direct callers retain the legacy fallback. | `performance_optimization_60_c_check.gd`, `ai_play_round103_check.gd`, `ai_play_round1_check.gd` |
| 104 | `ai_brain.gd.part` / completion probe ordering | Every standard completion probe checked the full seven-pairs and thirteen-orphans scans before attempting the ordinary four-meld decomposition. | Try the common standard decomposition first and inspect alternate hand families only after it fails, preserving the original special-hand fallbacks and count restoration. | `performance_optimization_60_c_check.gd`, `ai_play_round104_check.gd`, `ai_play_round77_check.gd`, `offline_smoke_test.gd` |
| 105 | `core.gd.part`, `main_base.gd` / standard shanten recursion | A standard-hand shanten search continued exploring sibling decompositions after one branch had already found the lowest possible result, `-1`. | Treat `-1` as a hard lower bound, restore each branch's temporary count mutation, memoize the lower-bound result, and return immediately; keep the diagnostic hit counter reset with the shanten cache. | `ai_play_round105_check.gd`, `performance_optimization_60_c_check.gd`, `offline_smoke_test.gd` |
| 106 | `gameplay.gd.part`, `ai_brain.gd.part` / AI self-draw validation | Each AI draw loop first called `can_win_for_seat()` and then the tsumo report repeated the complete-hand validation; the count-based path also used ron scoring semantics for a no-extra self-draw. | Let the tsumo report own the single validation and expose `win_valid` to both AI loops, then pass an explicit self-draw scoring flag through the count-based validator; preserve stale-draw rejection and all acceptance/pass decisions. | `ai_play_round106_check.gd`, `ai_play_round24_check.gd`, `ai_play_round40_check.gd`, `ai_play_round48_check.gd` |
| 107 | `ai_brain.gd.part` / claim-time ron validation | `get_claim_options()` validated a legal ron and `choose_ai_claim()` immediately ran the same complete-hand and furiten checks again before scoring that response. | Pass a private prevalidated flag from the claim chooser to `ai_ron_decision_report`; direct report callers retain the full validation and completed self-draw furiten fallback, while the same claim-options snapshot supplies the trusted result. | `ai_play_round107_check.gd`, `ai_play_round103_check.gd`, `ai_play_round40_check.gd`, `performance_optimization_60_c_check.gd` |
| 108 | `ai_brain.gd.part` / `effective_tile_metrics` | For a known tenpai result, every non-winning tile first failed the exact completion probe and then launched another full shanten search even though no result below tenpai was possible except a complete hand. | When the known shanten is `0` or lower, accept only the exact completion probe; reserve the shanten search for hands above tenpai, preserving standard, seven-pairs, and thirteen-orphans waits while removing one recursive search per non-winning wait candidate. | `ai_play_round108_check.gd`, `ai_play_round101_check.gd`, `performance_optimization_60_c_check.gd` |
| 109 | `ai_brain.gd.part` / discard defense weighting | Each candidate in a discard report recalculated the same wall progress, score-based defense adjustment, and profile risk factor even though these values are fixed for the evaluation pass. | Snapshot those invariant inputs in the existing evaluation context and let complete reports, quiet ranking, and post-claim fast reports reuse them; retain the live fallback for callers without a context. | `ai_play_round109_check.gd`, `ai_play_round100_check.gd`, `performance_optimization_60_c_check.gd` |
| 110 | `ai_brain.gd.part`, `core.gd.part` / validated AI win scoring | AI ron/tsumo reports validated the exact winning state, then the scorer repeated inventory, completion, and minimum-fan gating; the ron path also built an unused winning-hand array beside the count snapshot. | Pass the already validated count state through the scorer's existing `assume_complete` path, retain all fan calculations, and leave the public scorer's default validation unchanged for untrusted callers. | `ai_play_round110_check.gd`, `ai_play_round101_check.gd`, `ai_play_round103_check.gd`, `performance_optimization_60_c_check.gd` |
| 111 | `ai_brain.gd.part`, `core.gd.part` / count-based completion probes | Wait valuation and the general count-based scorer copied the 34-slot hand vector before calling a completion helper that already restores every temporary pair/set mutation. | Probe the caller-owned reusable count vector in place, preserving the helper's restoration contract and keeping the defensive copy for no longer-needed work out of the candidate loop. | `ai_play_round111_check.gd`, `ai_play_round101_check.gd`, `ai_play_round110_check.gd`, `performance_optimization_60_c_check.gd` |
| 112 | `ai_brain.gd.part` / claim candidate strategy weighting | A single claim response could evaluate several gang/peng/chi candidates, while each report and score re-read the same seat-static attack, claim, risk, and route parameters. | Snapshot those strategy values in the existing claim context, carry them into reports and action scoring, and let claim bonus/threshold helpers consume the shared aggression value with live fallbacks for context-free callers. | `ai_play_round112_check.gd`, `performance_optimization_60_c_check.gd` |
| 113 | `ai_brain.gd.part` / self-gang candidate strategy weighting | Each legal concealed/added gang candidate was scored twice, and both passes re-read the same seat-static attack, gang aggression, wait focus, and difficulty values. | Snapshot those values in the self-gang selector context, carry them into the report, and let both scoring passes use the snapshot with live fallbacks for standalone reports. | `ai_play_round113_check.gd`, `performance_optimization_60_c_check.gd` |
| 114 | `ai_brain.gd.part` / discard candidate fixed inputs | Each candidate report re-read the same difficulty, wall count, wait focus, route focus, profile labels, and risk factor while applying defensive and phase adjustments. | Snapshot the fixed discard-evaluation inputs in the existing evaluation context, pass them through candidate-level adjustment helpers, and retain live fallbacks for standalone callers. | `ai_play_round114_check.gd`, `performance_optimization_60_c_check.gd` |
| 115 | `audio.gd.part` / `keep_background_music_alive` | The per-frame BGM watchdog queried runtime audio availability even while its retry deadline had not arrived or an async start was already in flight. | Check the retry deadline and in-flight state before the runtime query, and throttle unavailable-runtime retries to the existing one-second watchdog interval. | `ai_play_round115_check.gd`, `performance_optimization_60_c_check.gd` |
| 116 | `ai_brain.gd.part` / discard wait and player-pressure reports | Every tenpai discard candidate rebuilt the same exposed-meld count, and player-target pressure re-read the same human readiness score. | Carry human readiness and exposed-meld snapshots in the discard evaluation context, reuse the latter in wait valuation, and retain live fallbacks for direct callers. | `ai_play_round116_check.gd`, `performance_optimization_60_c_check.gd` |
| 117 | `ai_brain.gd.part` / discard report inner-loop inputs | A tenpai discard report re-read the live wait focus after the evaluation context had already captured it, re-clamped difficulty inside the danger branch, and `human_readiness_for_defense()` queried the wall twice on a cache miss. | Reuse the context wait-focus snapshot and the report's parsed difficulty, and read the wall once before building the readiness cache key while retaining all standalone fallbacks. | `ai_play_round117_check.gd`, `performance_optimization_60_c_check.gd` |
| 118 | `ai_brain.gd.part` / discard feed penalty weighting | Each candidate's feed-risk penalty normalized the same difficulty again inside `discard_feed_penalty_weight()`, despite the report already having a parsed difficulty snapshot. | Pass the report/context difficulty snapshot through the feed-penalty helper and retain its live normalization fallback for context-free callers. | `ai_play_round118_check.gd`, `performance_optimization_60_c_check.gd` |
| 119 | `core.gd.part` / targeted threat-card safe-tile ranking | A per-opponent threat card built the general all-opponent safety and risk reports for every hand tile, then replaced those values with the targeted opponent's results. | Skip the preliminary general reports for targeted cards, keep the required all-opponent safe check, and compute the targeted risk directly; retain the existing general path for unscoped threat cards. | `ai_play_round119_check.gd`, `performance_optimization_60_c_check.gd` |
| 120 | `core.gd.part`, `ai_brain.gd.part` / threat/readiness wall inputs | Threat-table cache-key construction and plan-based opponent readiness each read the unchanged wall count twice in one calculation. | Snapshot the wall count once and reuse it for both cache-key strings or readiness terms, preserving the existing key and score values. | `ai_play_round120_check.gd`, `performance_optimization_60_c_check.gd` |
| 121 | `ai_brain.gd.part`, `core.gd.part` / visible-count cache-key construction | Evaluation-context creation and a cache-miss threat-table key both compacted the same 34-slot visible-count vector more than once. | Build the compact key once per evaluation context, pass it through the existing threat-key API, and reuse it when formatting the key while retaining the default calculation fallback. | `ai_play_round121_check.gd`, `performance_optimization_60_c_check.gd` |
| 122 | `ai_brain.gd.part` / claim evaluation context key reuse | Claim-context construction compacted the visible-count snapshot a second time after the shared evaluation context had already built the key, and an omitted snapshot could overwrite that key with an empty string. | Keep the key produced by the shared evaluation context, eliminating the duplicate compaction and preserving the live-count fallback for snapshot-free claim callers. | `ai_play_round122_check.gd`, `performance_optimization_60_c_check.gd` |
| 123 | `ai_brain.gd.part` / AI tsumo decision difficulty | A self-draw decision normalized the global difficulty once for the easy-mode gate and again before risk/feed thresholds in the same report. | Capture the normalized difficulty at the first shared decision point and reuse it for both gates, preserving the standalone live difficulty behavior and clamping semantics. | `ai_play_round123_check.gd`, `performance_optimization_60_c_check.gd` |
| 124 | `ai_brain.gd.part` / effective-tile hand cache key | An effective-tile call without a known shanten compacted the same hand-count vector once inside shanten-cache lookup and again while building its own cache key. | Compute the hand compact key once, pass it through the optional shanten helper override, and reuse it for the effective-tile cache key while preserving legacy callers and count results. | `ai_play_round124_check.gd`, `performance_optimization_60_c_check.gd` |
| 125 | `ai_brain.gd.part` / discard report visible-count snapshot | A normal discard-report cache miss scanned the live visible counts to build its cache key and immediately scanned them again for the evaluation context. | Capture visible counts once before cache lookup, pass the snapshot into the cache-key helper, and reuse it after a miss while keeping quiet simulation lazy and legacy callers supported. | `ai_play_round125_check.gd`, `performance_optimization_60_c_check.gd` |
| 126 | `ai_brain.gd.part` / claim report wall inputs | A claim report's shared evaluation context already captured the wall count, but the opening gate and wall-draw discipline path read the live wall independently. | Reuse the context wall snapshot for both claim decisions, retain one standalone wall read for context-free callers, and preserve the discipline helper's explicit wall fallback. | `ai_play_round126_check.gd`, `performance_optimization_60_c_check.gd` |
| 127 | `ai_brain.gd.part` / AI ron decision difficulty | A ron decision normalized the global difficulty separately for the easy and hard branches of its追分 threshold calculation. | Capture the normalized difficulty once within that branch and reuse it for both thresholds, preserving the live fallback and clamping behavior. | `ai_play_round127_check.gd`, `performance_optimization_60_c_check.gd` |
| 128 | `gameplay.gd.part`, `ai_brain.gd.part` / AI assistance visible-count snapshot | AI assistance captured one visible-count table snapshot for threat rendering, while the following discard-report cache miss rebuilt the same snapshot. | Pass the existing snapshot into discard report evaluation, keep the quiet all-Bot path lazy, and preserve the default report API for all other callers. | `ai_play_round128_check.gd`, `performance_optimization_60_c_check.gd` |
| 129 | `core.gd.part`, `ai_brain.gd.part` / threat-readiness wall snapshot | Opponent runtime readiness and each readiness report independently read the unchanged wall count during one threat evaluation pass; context-free reports also read it once for scoring and again for reason text. | Thread one optional wall-count snapshot through runtime-state construction, pressure fallbacks, readiness scoring, and readiness report reasons, while retaining live reads for callers without a snapshot. | `ai_play_round129_check.gd`, `performance_optimization_60_c_check.gd` |
| 130 | `core.gd.part`, `ai_brain.gd.part` / report/threat cache-key wall input | Report-key construction and evaluation-context construction each read the wall once for the nested threat-table key and again for their own state key or context field. | Add an optional wall-count snapshot to the threat-table and AI-report key helpers, pass the already captured value through both callers, and preserve the default live-read behavior. | `ai_play_round130_check.gd`, `performance_optimization_60_c_check.gd` |
| 131 | `ai_brain.gd.part`, `gameplay.gd.part` / AI assistance report/threat pass | The assistance refresh built a full discard evaluation context, discarded it after ranking, and rebuilt the same known-count, opponent, and threat-key inputs for the adjacent seat-threat render. | Export the completed report context through an optional output dictionary and reuse it for the threat render; keep cache-hit calls lazy and preserve the legacy report API for all other callers. | `ai_play_round131_check.gd`, `performance_optimization_60_c_check.gd` |
| 132 | `gameplay.gd.part`, `core.gd.part` / AI helper text report lookup | Hint, hand-tray, advice-summary, and detail-defense text already held the current discard reports but called `safest_discard_report()` without that snapshot, causing an avoidable cache lookup and deep report copy when the public advice was not yet committed. | Pass the existing report array into safest-discard selection and detail-defense text, preserving the legacy fallback for callers without a snapshot and keeping all displayed text unchanged. | `ai_play_round132_check.gd`, `performance_optimization_60_c_check.gd` |
| 133 | `render.gd.part`, `core.gd.part` / advisor defense card threat summary | Pending-claim and waiting advisor cards rendered the same opponent threat summary once inside defense text and again as the card subtitle, causing a redundant threat-cache lookup and report copy in every affected redraw. | Resolve the threat summary once per card payload, pass it through the internal defense-text formatter, and retain the existing direct defense API for callers without a summary snapshot. | `ai_play_round133_check.gd`, `performance_optimization_60_c_check.gd` |
| 134 | `core.gd.part`, `gameplay.gd.part`, `ai_brain.gd.part` / advisor helper threat report | Human hint, advice summary, and detailed defense text first queried the threat report while deciding whether to offer the safest discard, then queried it again to format the defense summary. | Carry the first threat report through safest-discard hint gating and summarize it directly, while retaining live-report fallbacks for public callers without a snapshot. | `ai_play_round134_check.gd`, `performance_optimization_60_c_check.gd` |
| 135 | `core.gd.part` / chat-panel candidate overlap scoring | Each of the four chat drawer candidates rescanned `MELD_LAYOUTS` and called `get_melds()` again, including a second pass for the upper-left exclusion rule. | Capture visible meld seats once per panel selection and pass that snapshot through all candidate scores; preserve the direct scorer's live fallback for callers without a snapshot. | `ai_play_round135_check.gd`, `performance_optimization_60_c_check.gd` |
| 136 | `render.gd.part`, `gameplay.gd.part`, `core.gd.part` / action-dock button-tree traversal | Action-dock drawing walked the nested action tree separately for the count, chrome identity signature, button layout, and pulse animation; the following layout finalization walked it again. | Collect one button snapshot per dock draw, thread it through count/signature/pulse/layout helpers, and retain live tree fallbacks for direct callers and empty-state paths. | `ai_play_round136_check.gd`, `performance_optimization_60_c_check.gd` |
| 137 | `render.gd.part`, `core.gd.part` / hand layout width | One hand render solved the required hand width for the baseline inset, then recomputed it for row metadata and fit validation; the fit helper also evaluated its content-size fallback even when metrics already carried that width. | Reuse the already solved `hand_required_width` for row metadata and `hand_layout_fits_content()`, and resolve `content_width` lazily; retain live width/content fallbacks for legacy callers. | `ai_play_round137_check.gd`, `performance_optimization_60_c_check.gd` |
| 138 | `render.gd.part` / hand interaction-state reads | One hand render repeatedly queried turn eligibility and pending-response state for the baseline, prompts, badges, row metadata, and every tile. | Capture the read-only interaction state once at the start of each fresh hand build, reuse it across all hand lanes and tiles, and take a fresh snapshot on later renders after state changes. | `ai_play_round138_check.gd`, `performance_optimization_60_c_check.gd` |
| 139 | `ai_brain.gd.part` / quiet post-claim visible-count snapshot | Each candidate in the quiet post-claim discard loop passed a newly allocated empty array to the safety and feed-risk helpers, even though the shared evaluation context already had the visible-count snapshot. | Add an optional visible-count snapshot to the fast post-claim report, pass the existing snapshot from candidate selection, and preserve the legacy empty-snapshot fallback for direct callers. | `ai_play_round139_check.gd`, `performance_optimization_60_c_check.gd` |
| 140 | `ai_brain.gd.part` / concealed self-gang effective-tile visibility | The concealed self-gang report compared before/after ukeire with two empty visible-count arguments, rebuilding the same public 34-slot count vector twice in the only branch that performs wait-narrowing checks. | Resolve the evaluation context's public visible-count snapshot once inside that branch and pass it to both effective-tile calculations, preserving the context-free live fallback and result values. | `ai_play_round140_check.gd`, `performance_optimization_60_c_check.gd` |
| 141 | `ai_brain.gd.part` / quiet discard pre-ranking risk visibility | The quiet Top-K pre-ranking loop discarded the visible-count snapshot and passed a fresh empty array to each candidate risk vector, despite already having the evaluation context and current visible-count state. | Pass the existing visible-count snapshot into each fast risk-vector calculation, preserving the legacy context-free fallback and bounded Top-K ranking. | `ai_play_round141_check.gd`, `performance_optimization_60_c_check.gd` |
| 142 | `ai_brain.gd.part`, `gameplay.gd.part` / deal-in risk score visibility | Quiet post-claim candidates and forced-discard telemetry routed an existing evaluation context through `deal_in_risk_score()`, which then allocated and passed an empty visible-count array to every risk-vector calculation. | Add an optional snapshot after the existing context argument, pass the already captured visible counts through quiet risk callers, and preserve the old context-only API and live fallback. | `ai_play_round142_check.gd`, `performance_optimization_60_c_check.gd` |
| 143 | `core.gd.part` / general threat-card visibility | The general threat-card loop already held one public visible-count snapshot, but each candidate's safety and risk helpers received a fresh empty array and rebuilt their fallback input. | Pass the loop snapshot into both candidate helpers, preserving the targeted-opponent branch and the context-free live fallback. | `ai_play_round143_check.gd`, `performance_optimization_60_c_check.gd` |
| 144 | `ai_brain.gd.part` / report-key profile remap memo | The report-key memo reused a key computed before an AI seat-profile remap because its input key omitted the profile mapping, and it also rebuilt the mapping string for the final key. | Include one captured profile-map key in the memo input and final report key, restoring remap invalidation while removing the duplicate mapping scan. | `ai_play_round22_check.gd`, `performance_optimization_60_c_check.gd` |
| 145 | `ai_brain.gd.part` / feed-risk visibility in tsumo and claim discipline | Tsumo continuation and fallback human-claim discipline passed an existing evaluation context to the feed-risk scorer with a fresh empty visible-count argument. | Thread the existing tsumo snapshot and the context-derived claim snapshot into feed-risk scoring, preserving context-free fallback behavior. | `ai_play_round145_check.gd`, `performance_optimization_60_c_check.gd` |
| 146 | `ai_brain.gd.part` / human-claim wall snapshot | Human-claim discipline read the wall once while computing player readiness and again for its late-game chi refusal branch. | Accept an optional wall snapshot, source it from the evaluation context or one live read, and reuse it for readiness caching and the late-game branch. | `ai_play_round146_check.gd`, `performance_optimization_60_c_check.gd` |
| 147 | `ai_brain.gd.part` / exposed-tile route evaluation | Every discard or claim candidate rebuilt the route feature input by rescanning the acting seat's existing meld arrays and converting each tile to an index. | Capture normalized existing-meld tile indices once in the evaluation context and reuse them for candidate route reports, while preserving live scans for context-free callers. | `ai_play_round147_check.gd`, `performance_optimization_60_c_check.gd` |
| 148 | `ai_brain.gd.part` / discard-pressure opponent loop | `discard_pressure_score()` queried the next seat's discard presence once for its special weight, then queried that same opponent again in the general opponent loop. | Resolve each opponent's discard presence once inside the existing loop and apply the next-seat extra weight in that branch, preserving the score and context-free fallback. | `ai_play_round148_check.gd`, `performance_optimization_60_c_check.gd` |
| 149 | `ai_brain.gd.part`, `core.gd.part` / feed-risk opponent loop | `discard_feed_risk_report()` queried the next seat's discard presence for chi risk, then queried it again when the same opponent entered the peng-risk loop. | Capture each opponent's discard count once per feed-risk report and pass it through the chi/peng helpers, preserving direct-call fallbacks and report contents. | `ai_play_round149_check.gd`, `performance_optimization_60_c_check.gd` |
| 150 | `core.gd.part` / all-opponent safety checks | Threat-card ranking and discard safety paths repeatedly scanned the same opponents for the same tile within one evaluation context. | Memoize the boolean all-opponent safety result by seat/tile in the existing context, while retaining the live scan for context-free callers. | `ai_play_round150_check.gd`, `performance_optimization_60_c_check.gd` |
| 151 | `core.gd.part` / suji safety checks | Discard risk, safety labels, and seat threat cards repeatedly checked the same opponent/tile suji relationship during one evaluation pass. | Memoize the boolean suji result by opponent/tile in the existing context, while retaining direct live checks for context-free callers. | `ai_play_round151_check.gd`, `performance_optimization_60_c_check.gd` |
| 152 | `ai_brain.gd.part` / opponent pattern-threat scoring | A discard risk vector could calculate a pattern threat for an opponent/tile, then human-target pressure or a single-opponent risk calculation recomputed the same scalar. | Memoize the result by opponent, tile, and visible count in the existing evaluation context, while retaining context-free scoring. | `ai_play_round152_check.gd`, `performance_optimization_60_c_check.gd` |
| 153 | `ai_brain.gd.part` / discard candidate shape and route metrics | Each fully scored discard candidate scanned its 34-slot counts once for shape quality and again for route features. | Add an opt-in fused feature scan for discard reports, reuse its shape fields and route features, and retain the independent public paths for other callers. | `ai_play_round153_check.gd`, `performance_optimization_60_c_check.gd` |
| 154 | `core.gd.part`, `render.gd.part` / hand-tray status text | One hand redraw computed `hand_tray_text()` for the detail snapshot, recomputed it for compact display, and queried it three more times for state-badge tooltip/full-text metadata. | Accept the already resolved detail text in `hand_tray_visible_text()`, reuse it for the compact label and all badge metadata, and retain the no-argument live fallback for existing callers. | `ai_play_round154_check.gd`, `performance_optimization_60_c_check.gd` |
| 155 | `core.gd.part`, `render.gd.part` / hand-tray state colors | One hand redraw resolved the state text for the badge, then `hand_tray_state_fill()` resolved it again and `hand_tray_state_border()` resolved both the fill and state a third time; decorative hand-tray art repeated the fill lookup. | Accept one state-text snapshot and, where available, one fill snapshot through the badge and hand-tray art helpers; retain no-argument live fallbacks for existing callers. | `ai_play_round155_check.gd`, `performance_optimization_60_c_check.gd` |
| 156 | `render.gd.part` / hand-tray decorative interaction state | The hand redraw already captured discard eligibility and pending-danger state, but momentum, completion, and suit-flow art re-read those values while constructing decorative nodes. | Thread optional active/danger snapshots through the hand-tray art helpers, preserving live fallbacks for direct callers and existing force flags. | `ai_play_round156_check.gd`, `performance_optimization_60_c_check.gd` |
| 157 | `ai_brain.gd.part` / self-gang wall discipline | Each self-gang candidate already carried an evaluation context with the current wall count, but the residual-wall discipline helper was called without it and read the wall again. | Pass the context wall snapshot into self-gang discipline and retain the live wall fallback for context-free reports. | `ai_play_round157_check.gd`, `performance_optimization_60_c_check.gd` |
| 158 | `render.gd.part` / meld danger-state lookup | `draw_melds()` read pending-danger state once for compact mode and immediately read it again for danger-specific compact sizing. | Capture one pending-danger snapshot for both layout branches, publish the derived state on each lane, and retain live behavior across redraws. | `ai_play_round158_check.gd`, `performance_optimization_60_c_check.gd` |
| 159 | `core.gd.part` / chat-panel occupancy geometry | Each of the four chat drawer candidates rebuilt the same seat, visible meld, river, table, HUD, action, hand, and ledger geometry; dynamic action geometry also repeated the button-count lookup. | Build one occupancy geometry snapshot outside the candidate loop and pass it through scoring, retaining candidate-specific ledger exclusion and legacy live/direct-call fallbacks. | `ai_play_round159_check.gd`, `performance_optimization_60_c_check.gd` |
| 160 | `core.gd.part` / pending-claim context occupancy geometry | Each pending-response context candidate rebuilt the same discard-river, meld, wall, and center-console geometry, and each wall lane re-read the safe content size. | Build one occupancy geometry snapshot before candidate clearance checks, reuse one viewport/content-size snapshot for layout decisions, and retain the direct candidate-clearance fallback. | `ai_play_round160_check.gd`, `performance_optimization_60_c_check.gd` |
| 161 | `core.gd.part` / advisor-panel occupancy geometry | Each of the four advisor side-channel candidates rebuilt the same seat, meld, wall, river, center, HUD, hand, action, and chat exclusion geometry. | Build one occupancy geometry snapshot outside the advisor candidate loop and pass it through clearance checks, retaining the direct live fallback. | `ai_play_round161_check.gd`, `performance_optimization_60_c_check.gd` |
| 162 | `render.gd.part` / table-log viewport reads | One table-log redraw re-read the same viewport for its signature, compact-mode branching, and three label-width calculations; archive rows repeated the viewport read for every record and history row. | Capture one viewport snapshot per ledger/archive draw, pass it into the render signature, reuse it for compact/layout sizing, and retain the default live fallback for direct signature callers. | `ai_play_round162_check.gd`, `performance_optimization_60_c_check.gd` |
| 163 | `render.gd.part` / action pending-state reads | One action-bar redraw independently checked the pending-claim window at multiple branch points and rechecked pending-danger state before building the normal AI actions. | Capture pending-claim and pending-danger state once after early recovery exits, reuse both snapshots across action lanes, and publish the state values on the action bar while preserving the live helper fallbacks. | `ai_play_round163_check.gd`, `performance_optimization_60_c_check.gd` |
| 164 | `render.gd.part` / center wind compass current-seat reads | One center wind compass redraw read the current seat for its simple markers, then read it again for the pointer and full wind-state markers. | Capture one current-seat snapshot at the start of the compass draw, reuse it across both marker layers and pointer rotation, and publish the snapshot policy while preserving fresh values across later redraws. | `ai_play_round164_check.gd`, `performance_optimization_60_c_check.gd` |
| 165 | `render.gd.part` / score-strip current-seat reads | One score-strip redraw queried the current seat separately for each of its four seat chips. | Capture one current-seat snapshot before the chip loop, reuse it for all active-state decisions, and publish the snapshot policy while preserving fresh values across later redraws. | `ai_play_round165_check.gd`, `performance_optimization_60_c_check.gd` |
| 166 | `render.gd.part` / score-strip delta text reads | Each score chip resolved `score_delta_text(seat)` for its visible label and then resolved it again in the tooltip condition. | Capture one score-delta text snapshot per chip, reuse it for the visible label and tooltip metadata, and preserve fresh values on later redraws. | `ai_play_round166_check.gd`, `performance_optimization_60_c_check.gd` |
| 167 | `render.gd.part` / score-strip viewport reads | One score-strip redraw resolved the same viewport separately inside each of its four seat-chip iterations. | Capture one viewport snapshot before the chip loop, reuse it for all compact-layout decisions, and publish the snapshot policy while preserving fresh values across later redraws. | `ai_play_round167_check.gd`, `performance_optimization_60_c_check.gd` |
| 168 | `render.gd.part` / center wind-label content-size reads | Each of the four center wind labels recomputed the same safe content size before fitting its font. | Capture one content-size snapshot before the wind-label loop, reuse it for all four font-fit budgets, and publish the snapshot policy while preserving fresh values across later redraws. | `ai_play_round168_check.gd`, `performance_optimization_60_c_check.gd` |
| 169 | `render.gd.part` / center draw cross-lane content-size reads | With a latest discard present, the center draw computed safe content size once for the discard label and again for the wind-label pass. | Capture one content-size snapshot for the whole center draw, reuse it for latest-discard and wind-label fitting, and publish both the draw-wide and wind-pass policies. | `ai_play_round169_check.gd`, `performance_optimization_60_c_check.gd` |
| 170 | `render.gd.part` / pending-claim illustration viewport reads | One pending-claim illustration used separate viewport reads for compact and narrow layout decisions. | Capture one viewport snapshot after the content geometry, reuse it for both layout branches, and publish the snapshot policy while preserving fresh values across later redraws. | `ai_play_round170_check.gd`, `performance_optimization_60_c_check.gd` |
| 171 | `gameplay.gd.part`, `render.gd.part` / action-dock pending-state reads | `draw_actions()` already captured pending-claim and pending-danger state, but each downstream action-dock draw and its chrome identity signature read those helpers again. | Pass optional pending-state snapshots through `draw_action_dock()` and `battle_action_chrome_identity_signature()`, publish the dock snapshot policy, and retain live fallbacks for direct callers. | `ai_play_round171_check.gd`, `performance_optimization_60_c_check.gd` |
| 172 | `screens.gd.part` / daily-login viewport reads | Each of the seven daily-login cards read the same viewport separately for date, state, and reward font fitting, and the page layout read its height again for the large-text branch. | Capture one viewport snapshot per daily-login build, reuse it for all seven cards and layout decisions, publish the panel snapshot policy, and preserve the completed snapshot across later state changes. | `ai_play_round172_check.gd`, `performance_optimization_60_c_check.gd` |
| 173 | `screens.gd.part` / shop viewport and content-size reads | One shop build reread viewport for responsive mode, text fitting, spacer sizing, and footer fitting; each item row reread content-size for its name width. | Capture one viewport and one content-size snapshot per shop build, reuse them across item sizing and footer layout, publish both panel snapshot policies, and preserve the completed snapshots across later state changes. | `ai_play_round173_check.gd`, `performance_optimization_60_c_check.gd` |
| 174 | `screens.gd.part` / loading-page viewport reads | One loading build reread viewport for the title, status text, optional error hint, and bottom tip fitting. | Capture one viewport snapshot per loading-page build, reuse it across normal and error text lanes, publish the panel snapshot policy, and preserve the completed snapshot across later state changes. | `ai_play_round174_check.gd`, `performance_optimization_60_c_check.gd` |
| 175 | `screens.gd.part` / online-lobby viewport reads | The online-lobby build reread viewport for wide layout, room-badge compactness, and log compactness; its initial state/content refresh then reread it for endpoint and room text fitting. | Capture one viewport snapshot per lobby build, pass it through the initial lobby-state and room-content refresh, retain live fallbacks for later no-argument refresh calls, and publish the panel snapshot policy. | `ai_play_round175_check.gd`, `performance_optimization_60_c_check.gd` |
| 176 | `screens.gd.part` / stats viewport and content-size reads | One stats build reread viewport for wide layout and content-size for the scroll-position status font budget after using the same values for row geometry. | Capture one viewport and one content-size snapshot per stats build, reuse them for row and status layout, publish both panel snapshot policies, and preserve the completed snapshots across later state changes. | `ai_play_round176_check.gd`, `performance_optimization_60_c_check.gd` |
| 177 | `screens.gd.part` / achievements viewport reads | One achievement build read the same viewport once for the row-height contract and again for the wide-gallery layout branch. | Capture one viewport snapshot per achievement build, pass it into the row-height contract, reuse it for wide-layout decisions, publish the panel snapshot policy, and preserve the no-argument live fallback for direct row callers. | `ai_play_round177_check.gd`, `performance_optimization_60_c_check.gd` |
| 178 | `render.gd.part` / `make_achievement_row` viewport reads | Each achievement row reread the same viewport for its row height and three text-fitting budgets while the page already held a build snapshot. | Accept the page viewport snapshot in each row, reuse it for row height and all fitting budgets, publish the row snapshot policy, and preserve the live fallback for direct row callers. | `ai_play_round178_check.gd`, `performance_optimization_60_c_check.gd` |
| 179 | `screens.gd.part`, `render.gd.part` / rules viewport reads | One rules build reread viewport for compact scroll layout, scrollbar pixel sizing, and narrow-screen rule copy; the guide art also read it independently. | Capture one viewport snapshot per rules build, pass it into guide art, reuse it across layout decisions, publish both panel and guide snapshot policies, and preserve the guide's live fallback for direct callers. | `ai_play_round179_check.gd`, `performance_optimization_60_c_check.gd` |
| 180 | `render.gd.part` / stats summary-chip viewport reads | Each of the three statistics summary chips reread the same viewport for its value, unit, and caption fitting budgets. | Resolve one dashboard viewport snapshot, pass it to all three chips, reuse it for each chip's three fitting budgets, publish dashboard/chip policies, and preserve the chip's live fallback for direct callers. | `ai_play_round180_check.gd`, `performance_optimization_60_c_check.gd` |
| 181 | `render.gd.part` / hand tutorial viewport read | The hand tray already held a viewport snapshot for compact art, but the tutorial hint width budget performed another viewport read during the same draw. | Reuse the existing hand viewport snapshot for the tutorial hint, publish the tray snapshot policy, and preserve fresh values on later hand redraws. | `ai_play_round181_check.gd`, `performance_optimization_60_c_check.gd` |
| 182 | `render.gd.part` / achievement completion-art viewport reads | The completion convergence art read the same viewport separately for its compact-width and compact-height layout checks. | Capture one completion-art viewport snapshot, reuse it for both compact-layout checks, and publish the per-draw snapshot policy. | `ai_play_round182_check.gd`, `performance_optimization_60_c_check.gd` |
| 183 | `render.gd.part` / settings overlay viewport read | The settings overlay already captured a viewport for compact-layout selection, but the rule status fitting budget read it again during the same draw. | Reuse the existing settings viewport snapshot for the rule status budget, publish it on the overlay, and preserve the modal's later state unchanged. | `ai_play_round183_check.gd`, `performance_optimization_60_c_check.gd` |
| 184 | `render.gd.part` / win-detail viewport reads | Settlement detail, its yaku lane, and the showcase each reread the same viewport during one draw. | Capture one detail viewport snapshot, reuse it for compact branches, pass it into the showcase, and preserve the showcase's live fallback for direct callers. | `ai_play_round184_check.gd`, `performance_optimization_60_c_check.gd` |
| 185 | `render.gd.part` / menu-card viewport reads | Each menu card reread viewport for compact subtitle typography and the title/subtitle fitting budgets. | Capture one card viewport snapshot, reuse it for all text budgets, publish the per-card snapshot policy, and preserve the existing text split and layout. | `ai_play_round185_check.gd`, `performance_optimization_60_c_check.gd` |
| 186 | `render.gd.part` / menu-card motion viewport read | Menu-card motion independently reread viewport twice for its compact animation branch after the card had already resolved its viewport. | Accept the card snapshot in the motion helper, reuse it for compact animation decisions, publish the motion snapshot policy, and retain the live fallback for unrelated direct callers. | `ai_play_round186_check.gd`, `performance_optimization_60_c_check.gd` |
| 187 | `render.gd.part` / setting-row viewport reads | Each setting row reread viewport for its title fit and compact consequence-status fit after resolving the row's compact layout. | Reuse the row viewport snapshot for both text budgets, publish the per-row snapshot policy, and preserve the existing large-text and compact branches. | `ai_play_round187_check.gd`, `performance_optimization_60_c_check.gd` |
| 188 | `screens.gd.part` / local settings-refresh viewport reads | A local setting update reread viewport for compact height and width after the state token had already established that an update was needed. | Capture one viewport snapshot per local update, reuse it for the compact branch, publish the update snapshot policy, and retain the token-based early return. | `ai_play_round188_check.gd`, `performance_optimization_60_c_check.gd` |
| 189 | `render.gd.part` / online-feedback viewport reads | The online feedback footer read the same viewport separately for its wide left and right bounds. | Capture one feedback viewport snapshot, reuse it for both bounds, publish the per-draw snapshot policy, and preserve the empty-feedback early return. | `ai_play_round189_check.gd`, `performance_optimization_60_c_check.gd` |
| 190 | `render.gd.part` / online-roster viewport reads | Every roster redraw reread viewport for each row's name and status fitting budgets. | Capture one roster viewport snapshot, reuse it across all seat rows, publish the per-draw snapshot policy, and preserve empty-seat rendering and touch targets. | `ai_play_round190_check.gd`, `performance_optimization_60_c_check.gd` |
| 191 | `screens.gd.part` / replay archive date parsing | A replay archive row parsed the same date for its context, visible primary label, and full tooltip path. | Parse the full date once, derive the compact display text from that snapshot, reuse both texts across the row, and publish the per-row date snapshot policy. | `ai_play_round191_check.gd`, `performance_optimization_60_c_check.gd` |
| 192 | `screens.gd.part` / toast viewport reads | Toast layout read viewport separately for the width budget and anchored height budget. | Capture one toast viewport snapshot, reuse it for both geometry calculations, publish the per-draw snapshot policy, and preserve the queue/empty-state paths. | `ai_play_round192_check.gd`, `performance_optimization_60_c_check.gd` |
| 193 | `core.gd.part` / rules-section viewport reads | Each rule section reread viewport for wide typography and its wrapped-text width budget. | Capture one section viewport snapshot, reuse it for both layout decisions, publish the per-draw snapshot policy, and preserve measured line heights and authored section structure. | `ai_play_round193_check.gd`, `performance_optimization_60_c_check.gd` |
| 194 | `screens.gd.part` / exit-confirm viewport reads | Exit confirmation reread viewport for compact height and width decisions in the same dialog build. | Capture one exit-dialog viewport snapshot, reuse it for the compact branch, publish the per-draw snapshot policy, and preserve the confirmation surface and controls. | `ai_play_round194_check.gd`, `performance_optimization_60_c_check.gd` |
| 195 | `screens.gd.part` / replay archive row viewport reads | Each replay archive row reread viewport for date fitting, result fitting, and compact action sizing. | Capture one viewport snapshot per archive row, reuse it for both label budgets and the compact action branch, publish the per-row snapshot policy, and preserve the row's labels and action controls. | `ai_play_round195_check.gd`, `performance_optimization_60_c_check.gd` |
| 196 | `ai_brain.gd.part` / AI evaluation visible-count state key | A normal evaluation context reused the shared visible-count result but rescanned every player's rivers and melds to rebuild the same cache state key. | Reuse the state key published by the shared visible-count cache for no-argument contexts, retain the live-key fallback for explicit external snapshots, and preserve the complete context fields. | `ai_play_round196_check.gd`, `performance_optimization_60_c_check.gd` |
| 197 | `screens.gd.part` / replay archive row date parsing | Archive filtering, row signature construction, and a row rebuild could parse the same timestamp separately before the row was mounted. | Compute the date once in the row-for-entry path, pass it through the row signature and builder, retain the direct-builder fallback, and preserve the full/display date metadata and tooltip. | `ai_play_round197_check.gd`, `performance_optimization_60_c_check.gd` |
| 198 | `core.gd.part`, `render.gd.part` / pending-claim action layout viewport reads | Pending response columns, button width, row height, and dock geometry reread viewport/content size through nested layout helpers during one action-dock draw. | Capture one viewport/content-size pair per action-dock layout, pass it through response capacity and dock/bar geometry, short-circuit pending dock placement before normal geometry, and preserve live fallback for direct callers. | `ai_play_round198_check.gd`, `performance_optimization_60_c_check.gd` |
| 199 | `render.gd.part` / battle contract HUD content-size reads | Battle contract registration read safe content size separately for the title and status HUD fitting budgets during one registration batch. | Capture one content-size snapshot per battle contract registration, reuse it for both HUD budgets, publish the registration policy, and preserve generation/structure short-circuit behavior. | `ai_play_round199_check.gd`, `performance_optimization_60_c_check.gd` |
| 200 | `render.gd.part` / discard river row sizing viewport reads | `draw_discards()` captured the viewport once, but each of the four river zones reread it through side-river compact row sizing. | Pass the draw viewport snapshot into visible-row sizing for all zones, publish the river snapshot policy, and preserve the live fallback for direct callers. | `ai_play_round200_check.gd`, `performance_optimization_60_c_check.gd` |
| 201 | `render.gd.part` / discard river chrome signature viewport reads | Each river art signature and populated owner-overlay signature reread the same viewport after `draw_discards()` had already captured it. | Thread the draw viewport snapshot through both signature chains, preserve the live fallback for direct callers, and keep retention invalidation tied to the same viewport value. | `ai_play_round201_check.gd`, `performance_optimization_60_c_check.gd` |
| 202 | `render.gd.part` / meld lane signature viewport reads | Each populated meld lane reread the same viewport while `draw_melds()` was already using one viewport for compact-layout selection. | Pass the draw viewport snapshot into meld retention signatures, publish the per-draw policy, and preserve the live fallback for direct signature callers. | `ai_play_round202_check.gd`, `performance_optimization_60_c_check.gd` |
| 203 | `gameplay.gd.part`, `render.gd.part` / top HUD identity viewport reads | Top HUD construction read viewport once while building its retention signature and again for compact layout and fitting. | Capture one viewport at the draw entry, pass it into the identity signature and reuse it for HUD layout, preserving the no-argument signature fallback. | `ai_play_round203_check.gd`, `performance_optimization_60_c_check.gd` |
| 204 | `gameplay.gd.part`, `render.gd.part`, `core.gd.part` / round-summary viewport and content-size reads | The round-summary draw read viewport separately for its retention signature and compact branch, while action-dock geometry could independently resolve the same viewport/content size. | Capture one viewport/content-size pair at the draw entry, pass it through the identity signature and action-dock geometry, reuse it for panel layout, and preserve live fallbacks for direct callers. | `ai_play_round204_check.gd`, `performance_optimization_60_c_check.gd` |
| 205 | `gameplay.gd.part`, `render.gd.part` / center identity viewport reads | Center rendering read viewport once through its retention identity signature and again for compact layout during the same draw. | Capture one viewport snapshot at the center draw entry, pass it into the identity signature and reuse it for center layout, publish the snapshot on the root and center shell, and preserve the no-argument identity fallback. | `ai_play_round205_check.gd`, `performance_optimization_60_c_check.gd` |
| 206 | `gameplay.gd.part`, `render.gd.part` / seat identity and layout viewport reads | Each seat draw read viewport through its identity signature and again for compact height; the full battle loop repeated that pair for all four seats. | Capture one viewport snapshot for the seat batch, pass it into each identity/draw path and local threat refresh, publish it on seat nodes, and preserve live fallbacks for direct callers. | `ai_play_round206_check.gd`, `performance_optimization_60_c_check.gd` |
| 207 | `render.gd.part` / nested win-detail viewport reads | Round-summary rendering already held a viewport snapshot, but the nested win-detail section independently resolved viewport before forwarding it to the showcase. | Pass the parent summary viewport snapshot into win-detail rendering, let the showcase consume that value, and preserve the helper's live fallback for standalone callers. | `ai_play_round207_check.gd`, `performance_optimization_60_c_check.gd` |
| 208 | `core.gd.part`, `render.gd.part` / pending-claim nested viewport reads | The pending-claim illustration captured viewport for its own branches, while context geometry and table bounds independently resolved the same viewport through nested helpers. | Pass the illustration snapshot through context geometry and table bounds, preserve the direct helper fallbacks, and keep the completed illustration snapshot stable across later state changes. | `ai_play_round208_check.gd`, `performance_optimization_60_c_check.gd` |
| 209 | `core.gd.part`, `gameplay.gd.part`, `render.gd.part` / advisor identity/layout geometry | Advisor redraws built the full occupancy-selected layout for the retention identity and rebuilt the same geometry before mounting the panel. | Capture one viewport/content/layout snapshot at the draw entry, reuse it for the identity and panel mount, publish the snapshot policy, and preserve live fallbacks for direct helpers. | `ai_play_round209_check.gd`, `performance_optimization_60_c_check.gd` |
| 210 | `core.gd.part` / single-opponent risk tile classification | Each candidate risk report reparsed the same tile through middle-number, terminal-or-honor, honor, and chi-feed helpers during one opponent-risk calculation. | Resolve one tile index and derive all four classification flags once after the early exits, then reuse them across the pressure adjustments while preserving invalid-tile behavior. | `ai_play_round210_check.gd`, `performance_optimization_60_c_check.gd` |
| 211 | `ai_brain.gd.part`, `core.gd.part` / pattern-threat tile index parsing | Each opponent risk path reparsed the candidate tile in pattern-threat scoring after the enclosing risk loop had already resolved its tile index; suit lookup repeated the same normalization as well. | Pass one tile-index snapshot through aggregate and single-opponent pattern-threat calls, derive suit and rank directly from it, retain cache/context behavior, and preserve no-argument fallbacks. | `ai_play_round211_check.gd`, `performance_optimization_60_c_check.gd` |
| 212 | `core.gd.part` / added-gang candidate tile index parsing | The added-gang selector traversed all canonical tile codes and resolved each code through `tile_index_normalized()` before checking the already aligned hand-count slot. | Scan the canonical tile-count slots directly while retaining normalized meld detection, tile order, return values, and invalid-seat behavior. | `ai_play_round212_check.gd`, `performance_optimization_60_c_check.gd` |
| 213 | `ai_brain.gd.part` / hand-plan canonical tile classification | Each populated canonical slot called `is_simple_number_tile()` and `is_thirteen_orphans_tile()`, repeating normalization/cache lookups even though the loop index already identified the exact tile. | Initialize tile metadata once when needed, derive both classifications from the canonical slot/rank, and retain all feature totals and cache behavior. | `ai_play_round213_check.gd`, `performance_optimization_60_c_check.gd` |
| 214 | `ai_brain.gd.part` / shape isolated-tile neighbor scan | Shape metrics computed the four-number neighbor checks and then called `is_isolated_shape_tile()` to scan those same neighbors again for every populated tile. | Reuse the neighbor-found flag already computed by each shape scan, retain the singleton/pair and honor rules, and preserve shape quality totals. | `ai_play_round214_check.gd`, `performance_optimization_60_c_check.gd` |
| 215 | `core.gd.part` / hand-tray AI-assist status reads | The offline low-wall hand-tray branch queried `player_ai_assist_enabled()` separately for the disabled-assistance label and the recommendation branch during one status-text build. | Capture one local AI-assist snapshot after the critical-wall early return, reuse it for both low-wall/recommendation branches, and preserve all online, pending, and critical-wall early exits. | `ai_play_round215_check.gd`, `performance_optimization_60_c_check.gd` |
| 216 | `gameplay.gd.part` / deferred AI-assistance enable-state reads | One deferred AI assistance pass queried the same enable state once while building threat reports and again while committing the result, despite having no suspension point between them. | Capture one enable-state snapshot after the offline turn guard, reuse it for both conditional lanes, and preserve deferred report calculation and disabled-state threat-display behavior. | `ai_play_round216_check.gd`, `performance_optimization_60_c_check.gd` |
| 217 | `render.gd.part` / normal action self-discard and AI-state reads | The normal offline action-bar path reread `can_self_discard()` for win, concealed-gang, added-gang, and recommendation lanes, and reread the AI-assist state for recommendation and hint lanes. | Capture one self-discard and one AI-assist snapshot after pending/early-return lanes, reuse them for all normal offline action branches, and preserve enabled/disabled action visibility. | `ai_play_round217_check.gd`, `performance_optimization_60_c_check.gd` |
| 218 | `ai_brain.gd.part` / incremental hand-plan tile classification | The exposed-meld feature update converted each canonical tile slot back to a tile string before checking simple-number and thirteen-orphans classification. | Derive number/rank classification directly from the canonical slot and reuse it for both feature lanes, preserving incremental pair/orphan state and invalid-slot behavior. | `ai_play_round218_check.gd`, `performance_optimization_60_c_check.gd` |
| 219 | `ai_brain.gd.part`, `core.gd.part` / feed-risk tile classification | Each candidate feed-risk report resolved the same tile index and number/rank classification separately for the chi path and up to three meld paths. | Capture one candidate tile index at the report boundary, pass it through both feed-risk helpers, and derive valid tile classification directly while retaining no-argument fallbacks and invalid-tile behavior. | `ai_play_round219_check.gd`, `performance_optimization_60_c_check.gd` |
| 220 | `core.gd.part` / suji safety tile-index classification | Suji safety first classified the tile with `is_number_tile()` and then resolved its index again for each opponent, including callers that already held the candidate index. | Capture one canonical tile index at the table/risk boundary, pass it through suji safety checks, and retain the direct-call fallback, normalized aliases, and invalid-opponent behavior. | `ai_play_round220_check.gd`, `performance_optimization_60_c_check.gd` |
| 221 | `core.gd.part` / kabe safety tile-index classification | Kabe safety first classified the tile and resolved its visible count, then resolved the same tile index again for each eligible opponent. | Capture one canonical tile index at the table/risk boundary, reuse it for the visible-count guard and each opponent check, and retain direct-call fallback, normalized aliases, and invalid-tile behavior. | `ai_play_round221_check.gd`, `performance_optimization_60_c_check.gd` |
| 222 | `ai_brain.gd.part`, `core.gd.part` / discard-pressure tile-index classification | Every discard-pressure candidate resolved its visible count, terminal/honor classification, and same-suit pressure through separate tile normalization paths for each opponent. | Capture one candidate tile index, derive visible/terminal classification from it, pass it through same-suit pressure checks, and retain direct-call fallback, normalized aliases, and invalid-tile behavior. | `ai_play_round222_check.gd`, `performance_optimization_60_c_check.gd` |
| 223 | `ai_brain.gd.part` / discard-report route adjustment tile-index reuse | Normal discard reports already carried a candidate tile index, but opening-efficiency and post-meld route adjustments each resolved that tile again and repeated honor/terminal classification. | Thread the candidate index into both route-adjustment helpers, derive classification from the snapshot, and retain direct-call fallback plus invalid-tile behavior. | `ai_play_round223_check.gd`, `performance_optimization_60_c_check.gd` |
| 224 | `ai_brain.gd.part` / claim tile-index classification | One AI claim response reparsed the claimed tile for meld bonus and opening/pressure policy branches. | Capture one candidate index in the claim report, derive terminal/honor classification once, pass it through meld-bonus and pressure helpers, and retain direct-call fallback plus invalid-tile behavior. | `ai_play_round224_check.gd`, `performance_optimization_60_c_check.gd` |
| 225 | `ai_brain.gd.part`, `core.gd.part` / opponent risk tile-index reuse | Each opponent branch in one candidate risk vector reparsed the same tile before evaluating safety and pattern threat. | Capture one candidate tile index at the risk-vector boundary and pass it through the single-opponent risk wrappers and writer, preserving direct-call fallback and invalid-tile behavior. | `ai_play_round225_check.gd`, `performance_optimization_60_c_check.gd` |
| 226 | `ai_brain.gd.part` / added-gang public river lookup | Added-gang chankan risk checked an opponent's river before entering the single-opponent risk component, which checked the same river again. | Let the risk component own the public-river early exit, reuse one tile index for all opponents, and preserve discarded-tile exclusion plus report fields. | `ai_play_round226_check.gd`, `performance_optimization_60_c_check.gd` |
| 227 | `core.gd.part` / targeted threat-safe candidate normalization | Each hand candidate in a targeted threat card independently resolved its index for visible counts, suji, kabe, and single-opponent risk. | Capture one candidate tile index and pass it through the visible-count and safety/risk helpers, preserving candidate bounds, direct fallbacks, and invalid-tile behavior. | `ai_play_round227_check.gd`, `performance_optimization_60_c_check.gd` |
| 228 | `ai_brain.gd.part` / discard-report candidate index reuse | A complete discard report could resolve the candidate index while rebuilding original counts, again for route adjustments, and once more when publishing the report. | Capture the candidate index at report entry and reuse it for original-count restoration, route helpers, and the returned report, preserving explicit-index priority and invalid fallback. | `ai_play_round228_check.gd`, `performance_optimization_60_c_check.gd` |
| 229 | `ai_brain.gd.part` / claim route extra-meld index reuse | Claim route reconstruction repeated `tile_index()` for every copy of a gang/peng tile in the extra-meld list. | Build an aligned extra-meld index snapshot once and consume it in the route report, preserving legacy string-based fallback and invalid extra-tile behavior. | `ai_play_round229_check.gd`, `performance_optimization_60_c_check.gd` |
| 230 | `ai_brain.gd.part`, `gameplay.gd.part`, `core.gd.part` / discard-reason index reuse | Final discard reason generation could parse the candidate again while checking isolated-shape status after the report already held its index. | Pass the report's candidate index through reason text and isolated-discard detection, preserving public fallback behavior and reason output. | `ai_play_round230_check.gd`, `performance_optimization_60_c_check.gd` |
| 231 | `core.gd.part` / targeted threat candidate classification reuse | Targeted threat-card scoring reclassified every candidate through suit, honor, and terminal helpers after its canonical index was already captured. | Derive number/suit/honor/terminal flags once from the candidate index and reuse them in route scoring, preserving aliases, bounds, and candidate ordering. | `ai_play_round231_check.gd`, `performance_optimization_60_c_check.gd` |
| 232 | `core.gd.part` / targeted threat candidate sort-index reuse | Targeted threat-card ranking resolved a candidate's sort key after already capturing its canonical tile index, repeating normalization and lookup work for standard tiles. | Reuse the captured index for canonical tiles and retain `tile_sort_index()` for flowers and invalid values, preserving normalized aliases, fallback ordering, bounds, and deterministic results. | `ai_play_round232_check.gd`, `performance_optimization_60_c_check.gd` |
| 233 | `ai_brain.gd.part` / self-gang tile classification snapshot reuse | Self-gang report scoring recalculated honor and terminal classification from the tile string on both score passes, and the final high-pressure guard recalculated terminal/honor status once more. | Store the canonical index and derived classifications in the report, consume them in the pressure guard and action score, and retain legacy fallbacks for direct score callers and invalid tiles. | `ai_play_round233_check.gd`, `performance_optimization_60_c_check.gd` |
| 234 | `ai_brain.gd.part` / self-gang selector tile-index reuse | Concealed- and added-gang selectors already held each candidate's canonical tile index, but report construction normalized the same candidate again. | Accept an optional tile-index snapshot in self-gang report construction and pass the selector's existing index, preserving direct-call fallback, invalid rejection, and report decisions. | `ai_play_round234_check.gd`, `performance_optimization_60_c_check.gd` |
| 235 | `ai_brain.gd.part` / self-gang selector hand-count snapshot reuse | Concealed-gang selectors already built a 34-slot hand-count vector, but each report re-scanned the hand with `count_tile()` before shanten work. | Use the context hand-count slot for concealed-gang validation when available and retain the live hand scan for context-free or missing-snapshot callers. | `ai_play_round235_check.gd`, `performance_optimization_60_c_check.gd` |
| 236 | `ai_brain.gd.part` / self-gang selector open-meld snapshot reuse | Each self-gang report reread the acting player's open-meld count even though every candidate in a selector batch shares the same meld state. | Publish the open-meld count in concealed- and added-gang selector contexts and consume it in report construction, retaining the live fallback for direct or legacy contexts. | `ai_play_round236_check.gd`, `performance_optimization_60_c_check.gd` |
| 237 | `ai_brain.gd.part` / self-gang selector added-candidate validity reuse | Each added-gang report rechecked hand presence and scanned all meld tiles through `can_added_gang()` even though the selector had already built the valid triplet candidate set for the batch. | Publish the normalized added-gang candidate set in the selector context and use it as the authoritative report gate, retaining the live validation fallback for direct or legacy contexts. | `ai_play_round237_check.gd`, `performance_optimization_60_c_check.gd` |
| 238 | `ai_brain.gd.part` / self-gang selector route-label reuse | Each self-gang candidate rebuilt the same before-hand plan report to recover only its route label, even though all candidates in one selector batch share the immutable hand and meld state. | Capture the before-plan label once in concealed- and added-gang selector contexts and consume it in reports, retaining the live report fallback for direct or legacy contexts. | `ai_play_round238_check.gd`, `performance_optimization_60_c_check.gd` |
| 239 | `ai_brain.gd.part` / self-gang selector before-shanten reuse | Each self-gang candidate recalculated the same before-hand shanten from the selector's unchanged hand-count vector. | Capture before-shanten once in concealed- and added-gang selector contexts and consume it in reports, retaining the live calculation fallback for direct or legacy contexts. | `ai_play_round239_check.gd`, `performance_optimization_60_c_check.gd` |
| 240 | `ai_brain.gd.part`, `core.gd.part` / discard safety tile-index reuse | Discard reports already held a candidate tile index, but safety-label visibility, suji, and kabe branches reparsed the same tile while deriving the label. | Thread the candidate index through full and fast discard safety evaluation, retain direct-call fallback behavior, and preserve normalized aliases and invalid-tile handling. | `ai_play_round240_check.gd`, `performance_optimization_60_c_check.gd` |
| 241 | `ai_brain.gd.part` / discard-pressure tile-index reuse | Full discard reports already held a candidate tile index, but discard-pressure visibility, terminal classification, and same-suit pressure reparsed the candidate. | Accept and forward the candidate index through pressure scoring, retaining direct-call fallback behavior and normalized alias results. | `ai_play_round241_check.gd`, `performance_optimization_60_c_check.gd` |
| 242 | `ai_brain.gd.part` / feed-risk tile-index reuse | Full and fast discard reports already held a candidate tile index, but aggregate feed-risk evaluation resolved the candidate again before its chi and meld branches. | Accept and forward the candidate index through feed-risk aggregation and its nested helpers, retaining direct-call fallback behavior and report contents. | `ai_play_round242_check.gd`, `performance_optimization_60_c_check.gd` |
| 243 | `ai_brain.gd.part` / risk-vector tile-index reuse | Full and fast discard reports already held a candidate tile index, but risk-vector visibility and each opponent risk component could resolve the candidate again. | Accept and forward the candidate index through deal-in risk and aggregate risk-vector evaluation, retaining direct-call fallback behavior and risk fields. | `ai_play_round243_check.gd`, `performance_optimization_60_c_check.gd` |

The ledger describes bounded work and reuse; it does not claim a benchmark
percentage without a controlled before/after profile.
